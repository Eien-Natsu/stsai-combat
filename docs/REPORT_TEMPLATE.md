# 目标机交付报告

日期与 commit：
执行 AI／环境：

## 验收状态

| 关卡 | 通过／失败／未执行 | 证据路径与命令 |
|---|---|---|
| G0 工程 | | |
| G1 目标 GPU | | |
| G2 native pilot | | |
| G3 原版一致 | | |
| G4 强度提高 | | |
| G5 完整战斗覆盖 | | |

## 实际环境

OS、Python、CPU／RAM、GPU／显存、驱动、torch／CUDA wheel、依赖冻结、上游原始 SHA／补丁 SHA、剩余磁盘。

## 能力边界

角色、难度、卡、敌人、药水、遗物、选择阶段、实机接口、信念模型假设、已知缺陷。不能只写“支持铁甲”。

## 训练

数据目录／backend／split／场景分布／样本数；teacher 与模型 hash；搜索预算／cutoff；模型参数量；优化步、损失、峰值显存；中断与恢复说明。

## 评测

预注册分布／seed manifest；每策略完整赢／输／截断；每场 utility、HP；配对区间；p50/p95 延迟；是否足以晋级，判断依据是什么。未知人类基准写未知，不捏造。

## 下一项阻塞

具体失败、最小复现、待修文件、重跑命令、完成标准。
