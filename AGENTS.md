# 给执行 AI 的工作约定

你是实施者，不是只写方案的顾问。目标：在用户的 5070 电脑上继续完成铁甲战士局内战斗 AI，并留下可复现的代码、模型、评测证据。原版 pilot 是起点，不是用户最终目标。

## 不得突破的边界

1. 不读取当前真实未来抽牌顺序、未公开的怪物随机决策、种子、RNG 状态或卡牌 UUID 作为策略／搜索特征；不把完全信息搜索直接当公平教师。真实环境保存随机数是允许的，策略接口泄露不允许。既往已观察历史允许使用。
2. `reference_v1` 只做工程 smoke。不得以它的胜率宣传游戏水平，不得把其权重重标记为 native 权重。原版依赖失败必须显式失败，不得自动替换测试环境。
3. 不删除失败测试、降低验收标准、手写“通过”日志、把 skip 算 pass。先修实现、再重跑。未执行的 native、CUDA、原版差分明确写“未验证”。
4. 不把未知卡、药水、遗物、选择动作当 END。新能力必须同时增加状态导出、合法动作、采样审计、模型编码和规则测试。
5. 不使用最终盲测数据训练／调参／筛模型；失败挖掘只针对 train / 开发用 val。一次测试看过后，不再称其对后续模型完全未见。
6. 不自动升级或降级显卡驱动、不重启、不修改系统安全设置、不安装游戏盗版、不覆盖用户存档、不上传用户文件。环境安装限制在本项目虚拟环境。系统包／重启若确实必要，清楚报告具体阻碍和所需授权。
7. 先小规模测量再扩大。默认 2 个 CPU worker，单次迭代最多按已记录参数执行；没有显式资源许可，不启动付费云 GPU、无限训练、无人监管的全测试重复扫。
8. 不接受“所有接口有函数名”作为完成。源码里的 Protocol 省略号是接口声明，不是待实现业务；范围外主动抛错是能力边界，不得用伪实现消除错误。

## 必须按顺序推进

**A / 机器接管。** 阅读项目文件。记录 OS、Python、CPU／RAM／磁盘、显卡具体型号与显存、驱动、CUDA wheel、当前 Git 状态。保存到 `runs/doctor.json` 与 `reports/target_environment.md`。不要重复问用户已知的“有没有5070”。容量和系统直接检测。

**B / 工程与 GPU。** 建立 `.venv`，选择官方 Blackwell 兼容 CUDA wheel；运行 `pip check`、完整 pytest、CPU smoke 和 CUDA smoke。CUDA 自检必须包含实际模型前向和反向。有 `no kernel image`、非有限损失、OOM 时，修依赖／配置并保留失败记录。

**C / 原版候选编译。** 下载并锁定上游完整 SHA，读 `native/bridge.cpp` 所用真实头文件，编译。此候选未在交付环境编译，你必须主动处理命名、签名、GCC/CMake/pybind 差异。改动写入 Git，修补上游时保留原 SHA、补丁和新 SHA。不要重新写一个玩具环境替代它。运行 native 测试与 UBSan；所有 skip 都须解释。

**D / 原版校验。** 先保证原游戏与模拟器具有相同初始状态和相同行为脚本，再比较动作后的 HP、格挡、能量、牌区、状态、敌人意图和结束状态。随机一致性另行验证。`compare_traces.py` 只负责已对齐轨迹比较，不负责场景注入；注入／重放工具是你必须实现的工作。若机器没有合法游戏，仍可完成模拟器 pilot，但 G3 必须保持未通过。

**E / 小规模训练。** 先 16 局收集探针与 profile，再 128/16/32 的单轮迭代。检查截断、死亡分布、覆盖、搜索 cutoff、样本体积、训练峰值显存、单步延迟。全赢且没有难样本时，调整训练起始 HP／卡组分布，不要把训练曲线当能力进展。收益不明显先修 teacher 与数据，不盲目堆参数。

**F / 强化与扩展。** 按 `docs/05_ROADMAP.md` 扩展全铁甲的战斗能力。保留公平性回归集。优先补消耗／选择／药水／遗物及敌人隐变量审计，而不是添加花哨 UI。纯网络用于低延迟，混合搜索用于高预算；分别评测。不承诺单卡一定达到超人类。

## 每阶段必须交付

`reports/target_status.md` 逐项写：关卡、执行命令、实际输出路径、通过／失败／未执行、当前覆盖、阻碍及具体修复。GPU 报告需给出设备名、显存峰值、模型参数量；训练报告包含数据数量、seed namespace、搜索预算、checkpoint SHA256；评测包含每组赢／输／截断、存活 HP、utility、p50/p95 延迟和区间。

最终回复必须清楚区分：已经能做、尚不能做、实测强度、如何启动、下一项阻塞。没有游戏联调日志，不写“已接入游戏”。没有强度基准，不写“超人类”。
