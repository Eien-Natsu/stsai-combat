# 直接交给能操作 5070 电脑的 AI

请接管随附的 `stsai-combat` 项目，在这台带 NVIDIA RTX 5070 的机器上**实际安装、执行、修复并推进开发**，不要只返回实施方案。我的目标是先做出《杀戮尖塔》局内战斗 AI，先铁甲战士，后续冲击顶尖人类；本阶段不做路线、选牌或商店。

先读 `README.md`、`AGENTS.md` 和 `docs/00_ACCEPTANCE.md`。包中 Python 参考环境的搜索→采集→训练→评测闭环已在 CPU 上跑通，但参考环境不是原版；C++ `sts_lightspeed` 接入是尚未编译验证的有限范围候选，不能假定开箱即用。你的任务包括把候选在目标机上编译、修复、验证，并继续扩展，而不是运行 smoke 后结束。

请按下面顺序动手：

1. 自动检测系统、CPU、内存、磁盘、5070 具体型号／显存和驱动，建立独立 Python 环境。执行 GPU doctor，确认实际 CUDA 前向／反向成功；使用项目给出的显存档位启动，不把“看得到 CUDA”当兼容验证。
2. 跑完整 pytest 和 smoke，保留原始日志。随后锁定上游 commit、编译 `native/bridge.cpp`，修复真实 API／编译差异，运行 native 测试和随机回放。不得在 native 失败时偷偷退回 reference。
3. 为已支持的原版战斗实现相同初始状态／动作脚本的原游戏重放和差分校验。`compare_traces.py` 只是比较器，原版场景注入还需要你完成。有合法原版才能进行该部分；缺少游戏时写清 G3 未完成，不编造通过。
4. 在 native pilot 上先跑小规模探针，再运行有上限的 128 训练局、16 验证局、32 评测局闭环。给出数据量、搜索吞吐、显存峰值、训练日志和启发式／搜索／纯网络／混合搜索的对比。小样本仅验证流程，不据此宣称超人类。
5. 利用上轮网络指导搜索，保留旧训练数据，迭代蒸馏；按 `docs/05_ROADMAP.md` 扩展到选择／消耗、多敌人、药水、遗物、全铁甲卡池及强敌。每扩大一项，都补状态、合法动作、信念采样和原版差分测试。
6. CommunicationMod 默认只观察，确认支持范围和协议后才打开执行。仅纯策略实机桥已经给出；从任意实机状态重建 native 搜索状态仍是后续任务，不得声称已支持实机 MCTS。

硬约束：不窥视隐藏 RNG／未来抽牌；不把玩具环境成绩报成游戏成绩；不删除失败测试或把 skip 算 pass；不训练最终盲测集；不静默忽略不支持的动作；不升级驱动、重启、覆盖存档、上传文件或购买云算力，除非我另行授权。

请持续把实际进度写入 `reports/target_status.md`。最终交付更新源码、可加载模型、可重复命令、配置／依赖／上游 SHA、完整评测和明确的能力边界。你可以自主修复代码和调整小规模实验，但不能替我宣布未验证的完成。
